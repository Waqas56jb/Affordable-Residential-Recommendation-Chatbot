# Dependencies – never commit
node_modules/

# Env and secrets – never commit (keep .env.example)
.env
.env.local
.env.*.local
!.env.example

# Build / cache / OS
dist/
.vite/
.DS_Store
*.log
npm-debug.log*
